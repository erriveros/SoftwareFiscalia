<div class="field mx-auto", style="width: 200px;">
<%= form_with(model: witness, local: false) do |form| %>
  <% if witness.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(witness.errors.count, "error") %> prohibited this witness from being saved:</h2>

      <ul>
        <% witness.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :rut %>
    <%= form.text_field :rut %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="field">
    <%= form.label :detalle %>
    <%= form.text_field :detalle %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
</div>