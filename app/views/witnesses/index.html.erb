<p id="notice"><%= notice %></p>

<h1>Witnesses</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Rut</th>
      <th>Age</th>
      <th>Detalle</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @witnesses.each do |witness| %>
      <tr>
        <td><%= witness.name %></td>
        <td><%= witness.rut %></td>
        <td><%= witness.age %></td>
        <td><%= witness.detalle %></td>
        <td><%= link_to 'Abrir', witness %></td>
        <td><%= link_to 'Editar', edit_witness_path(witness) %></td>
        <td><%= link_to 'Eliminar', witness, method: :delete, data: { confirm: 'Está seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Testigo', new_witness_path %>
